package: libffi-[0-9]*.rpm

deps:
  - libffi8_copyright

slices:
  libs:
    deps:
      - libc6_libs
    contents:
      /usr/lib64/libffi.so.8*:

  copyright:
    contents:
      /usr/share/licenses/libffi/LICENSE:
