package: keyutils-libs-[0-9]*.rpm

deps:
  - libkeyutils1_copyright

slices:
  libs:
    deps:
      - libc6_libs
    contents:
      /usr/lib64/libkeyutils.so.*:

  copyright:
    contents:
      /usr/share/licenses/keyutils-libs/LICENCE.LGPL:
