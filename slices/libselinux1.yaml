package: libselinux-[0-9]*.rpm

deps:
  - libselinux_copyright

slices:
  libs:
    deps:
      - libc6_libs
      - libpcre2_libs
    contents:
      /usr/lib64/libselinux.so*:

  copyright:
    contents:
      /usr/share/licenses/libselinux/LICENSE: